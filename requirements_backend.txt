# ── IMPORTANT ──────────────────────────────────────────────────────────────
# scikit-learn MUST be pinned to 1.6.1
# Your .pkl files were saved with sklearn 1.6.1 in Google Colab.
# Using any other version will cause the TF-IDF vectorizer to misbehave
# and return wrong predictions (all 99s or all 0s).
# ───────────────────────────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
joblib==1.4.2
numpy==1.26.4
scikit-learn==1.6.1
xgboost==2.1.1
